# DaoyouCode 配置示例
# 本项目优先使用 backend/config/llm_config.yaml 配置API密钥
# 此文件仅作为备用方案（当YAML未配置时才会读取环境变量）

# ========== 备用方案：环境变量配置 ==========
# 只有当 llm_config.yaml 中未配置时，才会读取以下环境变量

# 通义千问（阿里云）
QWEN_API_KEY=your_qwen_api_key_here

# DeepSeek
DEEPSEEK_API_KEY=your_deepseek_api_key_here

# OpenAI（可选）
OPENAI_API_KEY=your_openai_api_key_here

# Claude（可选）
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# ========== 调试选项 ==========

# 开启LLM请求调试日志（0=关闭，1=开启）
DEBUG_LLM=0

# 保存LLM请求到文件（0=关闭，1=开启）
DEBUG_LLM_REQUEST=0

# 日志级别: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# ========== 推荐配置方式 ==========
# 
# 请直接编辑 backend/config/llm_config.yaml 文件：
#
# providers:
#   qwen:
#     api_key: ["sk-xxx", "sk-yyy"]  # 支持多个key轮询
#     enabled: true
#   deepseek:
#     api_key: "sk-xxx"
#     enabled: false
#
# 优势：
# 1. 支持多个API Key轮询（负载均衡）
# 2. 可以配置多个提供商
# 3. 可以启用/禁用特定提供商
# 4. 配置更清晰，易于管理
