# DaoyouCode 项目概览

> 智能AI代码助手，基于18大核心系统

---

## 🎯 项目简介

DaoyouCode是一个企业级AI代码助手，具有完整的Agent系统、智能路由、记忆管理等18大核心系统。

**当前位置**: `backend/`

---

## 📁 核心目录结构

```
backend/
├── daoyoucode/              # 核心系统
│   ├── agents/              # Agent系统
│   │   ├── core/            # 核心组件
│   │   │   ├── agent.py     # Agent基类和注册表
│   │   │   ├── task.py      # 任务管理
│   │   │   ├── router.py    # 智能路由器
│   │   │   ├── memory.py    # 记忆系统
│   │   │   ├── context.py   # 上下文管理
│   │   │   └── ...
│   │   ├── orchestrators/   # 7种编排器
│   │   │   ├── simple.py
│   │   │   ├── workflow.py
│   │   │   ├── parallel.py
│   │   │   ├── conditional.py
│   │   │   ├── multi_agent.py
│   │   │   ├── parallel_explore.py
│   │   │   └── react.py
│   │   ├── llm/             # LLM客户端
│   │   │   ├── client_manager.py
│   │   │   ├── config_loader.py
│   │   │   └── clients/
│   │   └── memory/          # 记忆系统
│   └── tools/               # 25个工具
│       ├── file_tools.py
│       ├── search_tools.py
│       ├── git_tools.py
│       └── ...
├── cli/                     # CLI工具
│   ├── commands/            # 10个命令
│   │   ├── chat.py          # 交互式对话
│   │   ├── edit.py          # 单次编辑
│   │   ├── doctor.py        # 环境诊断
│   │   └── ...
│   ├── ui/                  # UI组件
│   └── utils/               # 工具函数
├── config/                  # 配置文件
│   ├── llm_config.yaml      # LLM API配置
│   └── agent_router_config.yaml
└── daoyoucode.py            # 启动脚本
```

---

## 🏗️ 18大核心系统

### 1. Agent系统
- **BaseAgent**: Agent基类
- **AgentRegistry**: Agent注册表
- 支持6个专业Agent（MainAgent, CodeAgent, PlanAgent等）

### 2. 编排器系统 (7种)
- **SimpleOrchestrator**: 简单直接执行
- **WorkflowOrchestrator**: 多步骤工作流
- **ParallelOrchestrator**: 并行执行
- **ConditionalOrchestrator**: 条件分支
- **MultiAgentOrchestrator**: 多Agent协作
- **ParallelExploreOrchestrator**: 并行探索
- **ReActOrchestrator**: 规划-执行-反思循环

### 3. 智能路由器
- **IntelligentRouter**: 自动选择最优编排器和Agent
- 基于任务特征智能匹配
- 支持自定义配置

### 4. 任务管理
- **TaskManager**: 全局任务管理
- 支持任务层次结构
- 状态追踪和统计

### 5. 记忆系统
- **MemoryManager**: 统一记忆管理
- 对话历史（LLM层）
- 用户偏好（Agent层）
- 任务历史（Agent层）

### 6. 上下文管理
- **ContextManager**: 上下文管理器
- 智能上下文窗口管理
- 自动压缩和优先级排序

### 7. 权限系统
- **PermissionManager**: 权限管理器
- 100+权限规则
- 支持通配符和优先级

### 8. 验证机制
- **VerificationManager**: 验证管理器
- 4级验证（NONE, BASIC, STANDARD, STRICT）

### 9-18. 其他系统
- Hook系统、插件系统、反馈系统、规划器、委托管理、会话管理、后台任务、恢复机制、行为指南、技能系统

---

## 🛠️ 工具系统 (25个工具)

### 文件操作 (6个)
- read_file, write_file, list_directory, search_files, create_directory, delete_file

### 搜索工具 (2个)
- grep_search, semantic_search

### Git操作 (4个)
- git_status, git_diff, git_commit, git_log

### 命令执行 (2个)
- run_command, run_shell

### Diff工具 (1个)
- generate_diff

### RepoMap (2个)
- generate_repo_map, analyze_dependencies

### LSP工具 (6个)
- lsp_diagnostics, lsp_hover, lsp_definition, lsp_references, lsp_symbols, lsp_completion

### AST工具 (2个)
- ast_search, ast_transform

---

## 🎨 CLI工具 (10个命令)

### 核心命令
1. **chat** - 交互式对话（支持真实AI）
2. **edit** - 单次编辑（支持真实AI）
3. **doctor** - 环境诊断
4. **config** - 配置管理
5. **session** - 会话管理
6. **agent** - Agent管理
7. **models** - 模型管理
8. **serve** - 启动服务器
9. **version** - 版本信息
10. **help** - 帮助文档

### 特色功能
- ✅ 真实AI对话（通义千问）
- ✅ 优雅降级机制
- ✅ 文件上下文管理
- ✅ 对话历史
- ✅ 模型切换
- ✅ Rich UI美化

---

## 🔑 API配置

### 配置文件
`backend/config/llm_config.yaml`

### 支持的提供商
- 通义千问 (qwen)
- DeepSeek
- OpenAI
- Claude (Anthropic)

### 配置方式
1. 编辑配置文件
2. 设置环境变量

---

## 🚀 快速开始

### 启动交互式对话
```bash
cd backend
.\venv\Scripts\activate
python daoyoucode.py chat
```

### 编辑文件
```bash
python daoyoucode.py edit main.py "添加日志功能"
```

### 环境诊断
```bash
python daoyoucode.py doctor
```

---

## 💡 核心特性

### 1. 智能路由
- 自动分析任务特征
- 智能选择编排器和Agent
- 提供决策理由和置信度

### 2. 记忆系统
- 多层记忆（对话、偏好、任务）
- 自动学习用户习惯
- 上下文感知

### 3. 优雅降级
- Agent不可用时自动切换模拟模式
- 完整的错误处理
- 用户体验不中断

### 4. 工具丰富
- 25个专业工具
- LSP集成
- Git操作
- 文件管理

### 5. 权限控制
- 100+权限规则
- 细粒度控制
- 安全可靠

---

## 📚 文档

- `CLI_COMPLETED.md` - CLI完成总结
- `CLI_AGENT_INTEGRATION_COMPLETE.md` - Agent集成完成
- `AGEN