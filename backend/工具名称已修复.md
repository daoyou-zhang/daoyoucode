# ✅ 工具名称已修复

## 问题

Agent尝试调用工具时报错：
- `grep_search` - 找不到
- `repo_map` - 找不到  
- `list_directory` - 找不到

## 原因

Skill配置中的工具名称与实际注册的工具名称不匹配：

| Skill配置 | 实际工具名称 | 状态 |
|-----------|-------------|------|
| grep_search | text_search / regex_search | ❌ 不匹配 |
| search_files | ❌ 不存在 | ❌ 不存在 |
| list_directory | list_files | ❌ 不匹配 |
| repo_map | repo_map | ✅ 匹配 |

## 解决方案

修改 `skills/chat-assistant/skill.yaml`，使用正确的工具名称：

```yaml
tools:
  - repo_map              # 生成代码地图
  - get_repo_structure    # 获取目录结构
  - read_file             # 读取文件
  - text_search           # 文本搜索（修正）
  - regex_search          # 正则搜索（新增）
  - write_file            # 写入文件
  - list_files            # 列出目录（修正）
```

## 所有可用工具

运行 `python backend/test_tool_registry.py` 查看所有25个已注册的工具：

### 文件操作（6个）
- read_file
- write_file
- list_files
- get_file_info
- create_directory
- delete_file

### 搜索工具（2个）
- text_search
- regex_search

### Git工具（4个）
- git_status
- git_diff
- git_commit
- git_log

### 命令执行（2个）
- run_command
- run_test

### Diff工具（1个）
- search_replace

### RepoMap工具（2个）
- repo_map
- get_repo_structure

### LSP工具（6个）
- lsp_diagnostics
- lsp_goto_definition
- lsp_find_references
- lsp_symbols
- lsp_rename
- lsp_code_actions

### AST工具（2个）
- ast_grep_search
- ast_grep_replace

## 测试

现在可以重新测试：

```bash
cd backend
python -m cli.app chat
```

然后输入：
```
你 › 这个项目的结构是什么？
```

应该能看到：
```
🔧 执行工具: repo_map
   参数: {'repo_path': '.'}
   ⏳ 正在执行...
   ✓ 执行完成

AI › [基于repo_map结果的回答]
```

## 为什么之前grep_search"成功"了？

从你的输出来看：
```
🔧 执行工具: grep_search
   ✓ 执行完成
```

这可能是因为：
1. LLM生成了工具调用请求
2. Agent尝试执行但找不到工具
3. 返回了错误信息
4. LLM继续尝试其他方法

实际上工具并没有真正执行成功，只是Agent继续了推理循环。

## 下一步

所有工具名称已修复，现在Agent应该能够：
1. ✅ 调用 repo_map 分析项目结构
2. ✅ 调用 text_search 搜索代码
3. ✅ 调用 read_file 读取文件
4. ✅ 调用 list_files 列出目录

开始测试吧！🚀
