{
  "name": "daoyoucode-frontend",
  "version": "0.1.0",
  "private": true,
  "description": "daoyoucode 前端 Monorepo",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:tui": "pnpm --filter @daoyoucode/tui dev",
    "dev:web": "pnpm --filter @daoyoucode/web dev",
    "dev:desktop": "pnpm --filter @daoyoucode/desktop dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "turbo": "^1.11.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@8.12.0"
}
