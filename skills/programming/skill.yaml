name: programming
version: 1.0.0
description: 编程专家服务

# 使用 ReAct 编排器（更适合工具调用）
orchestrator: react

# 使用编程Agent
agent: programmer

# Prompt配置（使用独立文件）
prompt:
  file: prompts/programmer.md  # 简洁版本

# LLM配置
llm:
  model: qwen-coder-plus
  temperature: 0.1
  max_tokens: 4000

# 中间件
middleware:
  - context_management

# 可用工具（仅使用注册表中存在的名称，避免运行时报 Tool not found）
tools:
  - read_file
  - batch_read_files      # 🆕 批量读取文件
  - write_file
  - batch_write_files     # 🆕 批量写入文件
  - list_files
  - get_file_info
  - text_search
  - regex_search
  - get_file_symbols
  - git_status
  - git_diff
  - search_replace

# 输入
inputs:
  - name: task
    type: string
    required: true
    description: 编程任务描述
  
  - name: code_context
    type: string
    required: false
    description: 相关代码上下文
  
  - name: language
    type: string
    required: false
    description: 编程语言

# 输出
outputs:
  - name: code
    type: string
    description: 生成的代码
  
  - name: explanation
    type: string
    description: 代码说明

# Hook配置
hooks:
  - logging
  - metrics
