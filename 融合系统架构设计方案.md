# daoyoucode - 架构设计方案

> **项目名称**: daoyoucode  
> **项目定位**: 新一代AI编程助手  
> **设计日期**: 2026年2月10日  
> **设计目标**: 融合 daoyouCodePilot、oh-my-opencode、OpenCode 三者优势，打造新一代AI编程助手

---

## 📋 目录

1. [设计理念](#设计理念)
2. [核心优势融合](#核心优势融合)
3. [系统架构设计](#系统架构设计)
4. [功能模块设计](#功能模块设计)
5. [技术栈选择](#技术栈选择)
6. [实施计划](#实施计划)
7. [差异化优势](#差异化优势)

---

## 1. 设计理念

### 1.1 核心原则

**从三个项目中提炼的核心理念**：

1. **中文优先** (from daoyouCodePilot)
   - 原生中文支持，不是翻译
   - 深度理解中文编程需求
   - 中文文档和社区

2. **极致生产力** (from oh-my-opencode)
   - 多智能体协作
   - 并行任务执行
   - 自动化到极致

3. **开放可扩展** (from OpenCode)
   - 100%开源
   - 模型无关
   - 插件生态

### 1.2 设计目标

```
┌─────────────────────────────────────────────────────────────┐
│              daoyoucode 设计目标                             │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ✅ 开箱即用 (daoyou的易用性)                               │
│  ✅ 功能强大 (oh-my-opencode的能力)                         │
│  ✅ 灵活扩展 (OpenCode的架构)                               │
│  ✅ 中文优先 (国产化支持)                                   │
│  ✅ 成本优化 (智能模型选择)                                 │
│  ✅ 团队协作 (配置共享)                                     │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```



---

## 2. 核心优势融合

### 2.1 从 daoyouCodePilot 吸收

#### ✅ 保留优势
1. **中文优化**
   - 中文Prompt工程
   - 中文指令理解
   - 中文文档体系

2. **国产LLM深度集成**
   - Qwen系列优化
   - DeepSeek系列优化
   - 智能模型选择

3. **完整工具链**
   - 8种编辑模式
   - Function Calling工具集
   - Repository Map (Tree-sitter)

4. **Skill系统**
   - 可扩展Skill架构
   - Prompt模板系统
   - 按需加载机制

#### 🔄 改进方向
- 将Python实现迁移到TypeScript（统一技术栈）
- 增强LSP支持
- 添加多智能体能力

### 2.2 从 oh-my-opencode 吸收

#### ✅ 保留优势
1. **多智能体编排**
   - Sisyphus主编排器
   - 专业智能体（Oracle, Librarian, Explore等）
   - 并行后台任务

2. **LSP/AST工具**
   - 完整LSP重构工具
   - AST-Grep集成
   - 代码结构分析

3. **生产力增强**
   - Todo持续执行器
   - Ralph Loop
   - Ultrawork模式

4. **Hook系统**
   - 25+ 内置Hooks
   - 生命周期拦截
   - 可扩展Hook机制

#### 🔄 改进方向
- 简化配置复杂度
- 增加中文优化
- 降低学习曲线

### 2.3 从 OpenCode 吸收

#### ✅ 保留优势
1. **开源架构**
   - MIT许可证
   - 社区驱动
   - 透明开发

2. **模型无关**
   - 多提供商支持
   - 灵活切换
   - 成本优化

3. **客户端/服务器架构**
   - 前后端分离
   - 远程访问
   - 多客户端支持

4. **扩展机制**
   - 插件系统
   - MCP支持
   - 配置系统

#### 🔄 改进方向
- 增强中文支持
- 添加更多内置功能
- 优化用户体验

### 2.4 融合创新点

#### 🆕 独特优势
1. **智能路由系统**
   - 自动选择最佳模型
   - 任务类型识别
   - 成本优化

2. **统一知识库**
   - 跨会话学习
   - 代码模式缓存
   - 团队知识共享

3. **混合智能体**
   - 中文智能体 + 英文智能体
   - 国产模型 + 国际模型
   - 本地模型 + 云端模型

4. **渐进式复杂度**
   - 新手模式（开箱即用）
   - 专业模式（高级功能）
   - 专家模式（完全定制）



---

## 3. 系统架构设计

### 3.1 整体架构

```
┌─────────────────────────────────────────────────────────────────────┐
│                        daoyoucode 系统架构                           │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│                          用户界面层                                  │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐          │
│  │ CLI      │  │ TUI      │  │ GUI      │  │ IDE插件  │          │
│  │ (快速)   │  │ (终端)   │  │ (可视化) │  │ (集成)   │          │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘          │
│       │             │             │             │                  │
└───────┼─────────────┼─────────────┼─────────────┼──────────────────┘
        │             │             │             │
┌───────▼─────────────▼─────────────▼─────────────▼──────────────────┐
│                        API网关层                                     │
├─────────────────────────────────────────────────────────────────────┤
│  • 统一API接口                                                       │
│  • 认证授权                                                          │
│  • 请求路由                                                          │
│  • 限流控制                                                          │
└───────┬─────────────────────────────────────────────────────────────┘
        │
┌───────▼─────────────────────────────────────────────────────────────┐
│                        智能编排层                                    │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌────────────────────────────────────────────────────────┐        │
│  │              主编排器 (Orchestrator)                    │        │
│  │  • 任务分析与分解                                       │        │
│  │  • 智能体调度                                           │        │
│  │  • 执行监控                                             │        │
│  │  • 结果聚合                                             │        │
│  └────────────────────────────────────────────────────────┘        │
│                          │                                          │
│         ┌────────────────┼────────────────┐                        │
│         │                │                │                        │
│  ┌──────▼──────┐  ┌─────▼──────┐  ┌─────▼──────┐                 │
│  │ 任务路由器  │  │ 模型选择器 │  │ 上下文管理 │                 │
│  │ (智能分配)  │  │ (成本优化) │  │ (知识库)   │                 │
│  └─────────────┘  └────────────┘  └────────────┘                 │
│                                                                      │
└───────┬──────────────────────────────────────────────────────────────┘
        │
┌───────▼─────────────────────────────────────────────────────────────┐
│                        智能体层                                      │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐          │
│  │ 中文编辑 │  │ 架构顾问 │  │ 代码探索 │  │ 文档专家 │          │
│  │ Agent    │  │ Agent    │  │ Agent    │  │ Agent    │          │
│  │ (Qwen)   │  │ (GPT)    │  │ (Grok)   │  │ (Claude) │          │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘          │
│                                                                      │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐          │
│  │ 重构专家 │  │ 测试专家 │  │ 前端专家 │  │ 后端专家 │          │
│  │ Agent    │  │ Agent    │  │ Agent    │  │ Agent    │          │
│  │ (Claude) │  │ (DeepSeek)│ │ (Gemini) │  │ (Qwen)   │          │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘          │
│                                                                      │
└───────┬──────────────────────────────────────────────────────────────┘
        │
┌───────▼─────────────────────────────────────────────────────────────┐
│                        工具层                                        │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐          │
│  │ LSP工具  │  │ AST工具  │  │ Git工具  │  │ 搜索工具 │          │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘          │
│                                                                      │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐          │
│  │ 文件操作 │  │ 命令执行 │  │ 测试工具 │  │ 代码分析 │          │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘          │
│                                                                      │
└───────┬──────────────────────────────────────────────────────────────┘
        │
┌───────▼─────────────────────────────────────────────────────────────┐
│                        基础设施层                                    │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐          │
│  │ LLM网关  │  │ 缓存系统 │  │ 存储系统 │  │ 监控系统 │          │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘          │
│                                                                      │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐          │
│  │ 配置管理 │  │ 日志系统 │  │ 插件系统 │  │ Hook系统 │          │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘          │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

### 3.2 核心组件设计

#### 3.2.1 主编排器 (Orchestrator)

```typescript
class Orchestrator {
  // 任务分析
  async analyzeTask(task: Task): Promise<TaskAnalysis> {
    return {
      language: detectLanguage(task.prompt),      // 中文/英文
      complexity: assessComplexity(task),         // 简单/中等/复杂
      taskType: classifyTask(task),               // 编辑/重构/探索/分析
      requiredTools: identifyTools(task),         // LSP/AST/Git等
      estimatedCost: estimateCost(task),          // Token成本估算
    }
  }
  
  // 智能体选择
  async selectAgents(analysis: TaskAnalysis): Promise<Agent[]> {
    const agents = []
    
    // 根据语言选择主智能体
    if (analysis.language === 'zh-CN') {
      agents.push(new ChineseEditorAgent())      // 中文编辑智能体
    } else {
      agents.push(new EnglishEditorAgent())      // 英文编辑智能体
    }
    
    // 根据任务类型添加专业智能体
    if (analysis.taskType === 'refactor') {
      agents.push(new RefactorAgent())           // 重构专家
    }
    if (analysis.taskType === 'explore') {
      agents.push(new ExploreAgent())            // 探索专家
    }
    
    // 根据工具需求添加辅助智能体
    if (analysis.requiredTools.includes('LSP')) {
      agents.push(new LSPAgent())                // LSP专家
    }
    
    return agents
  }
  
  // 任务执行
  async execute(task: Task): Promise<Result> {
    // 1. 分析任务
    const analysis = await this.analyzeTask(task)
    
    // 2. 选择智能体
    const agents = await this.selectAgents(analysis)
    
    // 3. 并行执行（如果可能）
    const results = await this.executeParallel(agents, task)
    
    // 4. 聚合结果
    return this.aggregateResults(results)
  }
}
```

#### 3.2.2 智能路由器 (Router)

```typescript
class TaskRouter {
  // 路由规则
  private rules: RoutingRule[] = [
    {
      condition: (task) => task.language === 'zh-CN' && task.complexity === 'simple',
      agent: 'ChineseEditorAgent',
      model: 'qwen-coder-plus',
      priority: 10
    },
    {
      condition: (task) => task.taskType === 'refactor',
      agent: 'RefactorAgent',
      model: 'claude-opus-4-5',
      tools: ['LSP', 'AST-Grep'],
      priority: 20
    },
    {
      condition: (task) => task.complexity === 'complex',
      agent: 'OrchestratorAgent',
      model: 'gpt-5.2',
      subAgents: ['Explore', 'Librarian', 'Oracle'],
      priority: 30
    }
  ]
  
  // 路由决策
  route(task: Task): RoutingDecision {
    // 匹配规则
    const matchedRules = this.rules
      .filter(rule => rule.condition(task))
      .sort((a, b) => b.priority - a.priority)
    
    // 返回最高优先级规则
    return matchedRules[0]
  }
}
```

#### 3.2.3 模型选择器 (ModelSelector)

```typescript
class ModelSelector {
  // 模型配置
  private models = {
    // 国产模型（成本低）
    'qwen-coder-plus': { cost: 0.001, speed: 'fast', quality: 'good' },
    'qwen-max': { cost: 0.002, speed: 'medium', quality: 'excellent' },
    'deepseek-coder': { cost: 0.0015, speed: 'fast', quality: 'good' },
    
    // 国际模型（质量高）
    'claude-opus-4-5': { cost: 0.015, speed: 'slow', quality: 'excellent' },
    'gpt-5.2': { cost: 0.01, speed: 'medium', quality: 'excellent' },
    'gemini-3-pro': { cost: 0.005, speed: 'fast', quality: 'good' },
  }
  
  // 智能选择
  select(task: Task, constraints: Constraints): string {
    // 根据任务复杂度
    if (task.complexity === 'simple') {
      return 'qwen-coder-plus'  // 简单任务用便宜模型
    }
    
    // 根据语言偏好
    if (task.language === 'zh-CN') {
      return task.complexity === 'complex' ? 'qwen-max' : 'qwen-coder-plus'
    }
    
    // 根据任务类型
    if (task.taskType === 'architecture') {
      return 'gpt-5.2'  // 架构设计用GPT
    }
    if (task.taskType === 'refactor') {
      return 'claude-opus-4-5'  // 重构用Claude
    }
    
    // 根据成本约束
    if (constraints.maxCost) {
      return this.selectByCost(task, constraints.maxCost)
    }
    
    // 默认选择
    return 'qwen-max'
  }
}
```

#### 3.2.4 知识库管理器 (KnowledgeBase)

```typescript
class KnowledgeBase {
  // 代码结构缓存
  private repoMaps: Map<string, RepoMap> = new Map()
  
  // 代码模式缓存
  private patterns: Map<string, CodePattern[]> = new Map()
  
  // 历史记录
  private history: TaskHistory[] = []
  
  // 团队知识
  private teamKnowledge: TeamKnowledge = {}
  
  // 获取Repository Map
  async getRepoMap(repoPath: string): Promise<RepoMap> {
    if (!this.repoMaps.has(repoPath)) {
      // 使用Tree-sitter生成
      const repoMap = await this.generateRepoMap(repoPath)
      this.repoMaps.set(repoPath, repoMap)
    }
    return this.repoMaps.get(repoPath)!
  }
  
  // 学习代码模式
  async learnPattern(code: string, context: Context): Promise<void> {
    const pattern = await this.extractPattern(code, context)
    const key = this.getPatternKey(context)
    
    if (!this.patterns.has(key)) {
      this.patterns.set(key, [])
    }
    this.patterns.get(key)!.push(pattern)
  }
  
  // 查询相似任务
  async findSimilarTasks(task: Task): Promise<TaskHistory[]> {
    return this.history
      .filter(h => this.calculateSimilarity(h.task, task) > 0.8)
      .sort((a, b) => b.similarity - a.similarity)
      .slice(0, 5)
  }
  
  // 共享团队知识
  async shareKnowledge(knowledge: Knowledge): Promise<void> {
    // 上传到团队知识库
    await this.uploadToTeam(knowledge)
    
    // 更新本地缓存
    this.teamKnowledge = {
      ...this.teamKnowledge,
      ...knowledge
    }
  }
}
```



---

## 4. 功能模块设计

### 4.1 核心功能模块

#### 模块1: 多智能体系统

```typescript
// 智能体基类
abstract class BaseAgent {
  name: string
  model: string
  temperature: number
  tools: Tool[]
  
  abstract async execute(task: Task): Promise<Result>
}

// 中文编辑智能体
class ChineseEditorAgent extends BaseAgent {
  constructor() {
    super()
    this.name = '中文编辑助手'
    this.model = 'qwen-coder-plus'
    this.temperature = 0.2
    this.tools = [
      new FileOperationTool(),
      new CodeSearchTool(),
      new GitTool()
    ]
  }
  
  async execute(task: Task): Promise<Result> {
    // 1. 理解中文指令
    const intent = await this.understandChineseIntent(task.prompt)
    
    // 2. 加载上下文
    const context = await this.loadContext(task.files)
    
    // 3. 生成编辑
    const edits = await this.generateEdits(intent, context)
    
    // 4. 应用编辑
    return await this.applyEdits(edits)
  }
}

// 架构顾问智能体
class ArchitectAgent extends BaseAgent {
  constructor() {
    super()
    this.name = '架构顾问'
    this.model = 'gpt-5.2'
    this.temperature = 0.3
    this.tools = [
      new CodeAnalysisTool(),
      new DiagramTool(),
      new DocumentationTool()
    ]
  }
  
  async execute(task: Task): Promise<Result> {
    // 1. 分析代码结构
    const structure = await this.analyzeStructure(task.files)
    
    // 2. 识别问题
    const issues = await this.identifyIssues(structure)
    
    // 3. 提供建议
    const recommendations = await this.generateRecommendations(issues)
    
    return { structure, issues, recommendations }
  }
}

// 重构专家智能体
class RefactorAgent extends BaseAgent {
  constructor() {
    super()
    this.name = '重构专家'
    this.model = 'claude-opus-4-5'
    this.temperature = 0.1
    this.tools = [
      new LSPTool(),
      new ASTGrepTool(),
      new TestTool()
    ]
  }
  
  async execute(task: Task): Promise<Result> {
    // 1. 使用LSP分析
    const symbols = await this.lspAnalyze(task.files)
    
    // 2. 计划重构步骤
    const plan = await this.planRefactoring(symbols)
    
    // 3. 执行重构
    const changes = await this.executeRefactoring(plan)
    
    // 4. 运行测试验证
    const testResult = await this.runTests()
    
    return { changes, testResult }
  }
}

// 代码探索智能体
class ExploreAgent extends BaseAgent {
  constructor() {
    super()
    this.name = '代码探索'
    this.model = 'grok-code'
    this.temperature = 0.5
    this.tools = [
      new GrepTool(),
      new RepoMapTool(),
      new DocumentationSearchTool()
    ]
  }
  
  async execute(task: Task): Promise<Result> {
    // 1. 快速搜索
    const searchResults = await this.fastSearch(task.query)
    
    // 2. 分析相关性
    const relevantFiles = await this.rankByRelevance(searchResults)
    
    // 3. 提取关键信息
    const insights = await this.extractInsights(relevantFiles)
    
    return { relevantFiles, insights }
  }
}
```

#### 模块2: 编辑引擎

```typescript
class EditEngine {
  // 8种编辑模式（from daoyou）
  private modes = {
    editblock: new EditBlockMode(),
    wholefile: new WholeFileMode(),
    udiff: new UnifiedDiffMode(),
    patch: new PatchMode(),
    ask: new AskMode(),
    architect: new ArchitectMode(),
    help: new HelpMode(),
    search: new SearchMode()
  }
  
  // 执行编辑
  async edit(task: EditTask): Promise<EditResult> {
    // 1. 选择编辑模式
    const mode = this.selectMode(task)
    
    // 2. 生成编辑
    const edits = await mode.generate(task)
    
    // 3. 预览diff
    const preview = await this.generatePreview(edits)
    
    // 4. 用户确认
    if (await this.confirmWithUser(preview)) {
      // 5. 应用编辑
      return await this.applyEdits(edits)
    }
    
    return { cancelled: true }
  }
  
  // 智能选择编辑模式
  private selectMode(task: EditTask): EditMode {
    // 根据任务类型选择
    if (task.type === 'question') return this.modes.ask
    if (task.type === 'architecture') return this.modes.architect
    if (task.type === 'search') return this.modes.search
    
    // 根据文件大小选择
    if (task.fileSize > 1000) return this.modes.editblock
    if (task.fileSize < 100) return this.modes.wholefile
    
    // 根据修改范围选择
    if (task.changeScope === 'small') return this.modes.editblock
    if (task.changeScope === 'large') return this.modes.wholefile
    
    // 默认使用editblock
    return this.modes.editblock
  }
}
```

#### 模块3: LSP/AST工具集

```typescript
class LSPManager {
  private servers: Map<string, LSPServer> = new Map()
  
  // LSP工具（from oh-my-opencode）
  async diagnostics(file: string): Promise<Diagnostic[]> {
    const server = await this.getServer(file)
    return await server.getDiagnostics(file)
  }
  
  async rename(file: string, position: Position, newName: string): Promise<WorkspaceEdit> {
    const server = await this.getServer(file)
    return await server.rename(file, position, newName)
  }
  
  async gotoDefinition(file: string, position: Position): Promise<Location[]> {
    const server = await this.getServer(file)
    return await server.gotoDefinition(file, position)
  }
  
  async findReferences(file: string, position: Position): Promise<Location[]> {
    const server = await this.getServer(file)
    return await server.findReferences(file, position)
  }
}

class ASTManager {
  // AST-Grep工具（from oh-my-opencode）
  async search(pattern: string, files: string[]): Promise<Match[]> {
    return await astGrep.search({
      pattern,
      paths: files,
      language: this.detectLanguage(files[0])
    })
  }
  
  async replace(pattern: string, replacement: string, files: string[]): Promise<Edit[]> {
    return await astGrep.replace({
      pattern,
      replacement,
      paths: files,
      language: this.detectLanguage(files[0])
    })
  }
}
```

#### 模块4: Hook系统

```typescript
class HookSystem {
  private hooks: Map<HookEvent, Hook[]> = new Map()
  
  // 注册Hook
  register(event: HookEvent, hook: Hook): void {
    if (!this.hooks.has(event)) {
      this.hooks.set(event, [])
    }
    this.hooks.get(event)!.push(hook)
  }
  
  // 触发Hook
  async trigger(event: HookEvent, context: HookContext): Promise<HookContext> {
    const hooks = this.hooks.get(event) || []
    
    let currentContext = context
    for (const hook of hooks) {
      if (hook.enabled) {
        currentContext = await hook.execute(currentContext)
      }
    }
    
    return currentContext
  }
}

// 内置Hooks（from oh-my-opencode）
const builtinHooks = [
  // 上下文注入
  new DirectoryAgentsInjectorHook(),
  new DirectoryReadmeInjectorHook(),
  new RulesInjectorHook(),
  
  // 生产力控制
  new KeywordDetectorHook(),
  new ThinkModeHook(),
  new RalphLoopHook(),
  new TodoContinuationEnforcerHook(),
  
  // 质量安全
  new CommentCheckerHook(),
  new ThinkingBlockValidatorHook(),
  new EditErrorRecoveryHook(),
  
  // 恢复稳定
  new SessionRecoveryHook(),
  new ContextWindowLimitRecoveryHook(),
  
  // 通知UX
  new AutoUpdateCheckerHook(),
  new BackgroundNotificationHook(),
  new SessionNotificationHook(),
]
```

#### 模块5: Skill系统

```typescript
class SkillRegistry {
  private skills: Map<string, Skill> = new Map()
  
  // 加载Skill（from daoyou）
  async loadSkills(): Promise<void> {
    // 系统级Skills
    await this.loadSystemSkills()
    
    // 项目级Skills
    await this.loadProjectSkills()
    
    // 用户级Skills
    await this.loadUserSkills()
  }
  
  // 获取Skill
  getSkill(name: string): Skill | undefined {
    return this.skills.get(name)
  }
  
  // 触发Skill
  async triggerSkill(name: string, context: SkillContext): Promise<SkillResult> {
    const skill = this.getSkill(name)
    if (!skill) {
      throw new Error(`Skill not found: ${name}`)
    }
    
    return await skill.execute(context)
  }
}

// 内置Skills
const builtinSkills = [
  // 编辑Skills（from daoyou）
  new EditBlockSkill(),
  new WholeFileSkill(),
  new AskSkill(),
  new ArchitectSkill(),
  
  // 专业Skills（from oh-my-opencode）
  new PlaywrightSkill(),
  new GitMasterSkill(),
  new FrontendUIUXSkill(),
]
```



---

## 5. 技术栈选择

### 5.1 核心技术栈

```yaml
语言: TypeScript
运行时: Bun (性能优先) / Node.js (兼容性)
包管理: pnpm
构建工具: Turbo

前端:
  CLI: Commander.js
  TUI: Ink (React for CLI)
  GUI: Electron + React + TailwindCSS
  
后端:
  框架: Hono (轻量级)
  数据库: SQLite (本地) + PostgreSQL (团队)
  缓存: Redis (可选)
  
代码分析:
  LSP: vscode-languageserver
  AST: @ast-grep/napi
  Tree-sitter: tree-sitter + 各语言grammar
  
LLM集成:
  统一接口: LiteLLM风格
  国产模型: 直接SDK集成
  国际模型: OpenAI SDK兼容
  
测试:
  单元测试: Vitest
  集成测试: Playwright
  E2E测试: Cypress
```

### 5.2 项目结构

```
daoyoucode/
├── packages/
│   ├── core/                    # 核心包
│   │   ├── src/
│   │   │   ├── orchestrator/   # 编排器
│   │   │   ├── agents/         # 智能体
│   │   │   ├── tools/          # 工具集
│   │   │   ├── hooks/          # Hook系统
│   │   │   ├── skills/         # Skill系统
│   │   │   └── utils/          # 工具函数
│   │   └── package.json
│   │
│   ├── cli/                     # CLI工具
│   │   ├── src/
│   │   │   ├── commands/       # 命令
│   │   │   ├── ui/             # UI组件
│   │   │   └── index.ts
│   │   └── package.json
│   │
│   ├── tui/                     # 终端UI
│   │   ├── src/
│   │   │   ├── components/     # React组件
│   │   │   ├── screens/        # 屏幕
│   │   │   └── index.tsx
│   │   └── package.json
│   │
│   ├── gui/                     # 桌面应用
│   │   ├── src/
│   │   │   ├── main/           # Electron主进程
│   │   │   ├── renderer/       # React渲染进程
│   │   │   └── shared/         # 共享代码
│   │   └── package.json
│   │
│   ├── server/                  # 服务器（可选）
│   │   ├── src/
│   │   │   ├── api/            # API路由
│   │   │   ├── services/       # 业务逻辑
│   │   │   └── index.ts
│   │   └── package.json
│   │
│   ├── lsp/                     # LSP集成
│   │   ├── src/
│   │   │   ├── servers/        # LSP服务器
│   │   │   ├── tools/          # LSP工具
│   │   │   └── index.ts
│   │   └── package.json
│   │
│   ├── ast/                     # AST工具
│   │   ├── src/
│   │   │   ├── parsers/        # 解析器
│   │   │   ├── queries/        # 查询
│   │   │   └── index.ts
│   │   └── package.json
│   │
│   └── shared/                  # 共享代码
│       ├── src/
│       │   ├── types/          # 类型定义
│       │   ├── constants/      # 常量
│       │   └── utils/          # 工具函数
│       └── package.json
│
├── plugins/                     # 插件
│   ├── vscode/                 # VS Code插件
│   ├── jetbrains/              # JetBrains插件
│   └── vim/                    # Vim插件
│
├── skills/                      # 内置Skills
│   ├── editblock/
│   ├── wholefile/
│   ├── git-master/
│   └── playwright/
│
├── docs/                        # 文档
│   ├── zh-CN/                  # 中文文档
│   ├── en-US/                  # 英文文档
│   └── api/                    # API文档
│
├── examples/                    # 示例
│   ├── basic/
│   ├── advanced/
│   └── custom-agent/
│
├── tests/                       # 测试
│   ├── unit/
│   ├── integration/
│   └── e2e/
│
├── scripts/                     # 脚本
│   ├── build.ts
│   ├── test.ts
│   └── release.ts
│
├── .github/                     # GitHub配置
│   ├── workflows/              # CI/CD
│   └── ISSUE_TEMPLATE/
│
├── turbo.json                   # Turbo配置
├── package.json                 # 根package.json
├── tsconfig.json                # TypeScript配置
├── LICENSE                      # MIT许可证
└── README.md                    # 项目说明
```

### 5.3 依赖管理

```json
{
  "dependencies": {
    // 核心依赖
    "@ast-grep/napi": "^0.40.0",
    "tree-sitter": "^0.21.0",
    "vscode-languageserver": "^9.0.0",
    
    // LLM集成
    "@anthropic-ai/sdk": "^0.30.0",
    "openai": "^4.70.0",
    "@google/generative-ai": "^0.21.0",
    
    // 国产模型SDK
    "@alicloud/dashscope": "^1.0.0",
    "deepseek-sdk": "^1.0.0",
    
    // UI框架
    "commander": "^14.0.0",
    "ink": "^5.0.0",
    "react": "^19.0.0",
    
    // 工具库
    "zod": "^4.1.0",
    "remeda": "^2.26.0",
    "picocolors": "^1.1.0"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "vitest": "^3.0.0",
    "turbo": "^2.5.0",
    "@types/node": "^22.0.0"
  }
}
```



---

## 6. 实施计划

### 6.1 开发阶段

#### 阶段0: 项目初始化（1周）

**目标**: 搭建项目基础架构

**任务**:
- [ ] 创建Monorepo结构
- [ ] 配置TypeScript和构建工具
- [ ] 设置CI/CD流程
- [ ] 编写项目文档框架
- [ ] 设计Logo和品牌

**交付物**:
- ✅ 可运行的项目骨架
- ✅ 基础文档
- ✅ CI/CD配置

---

#### 阶段1: 核心引擎（4-6周）

**目标**: 实现核心编排和智能体系统

**任务**:
- [ ] 实现Orchestrator核心逻辑
- [ ] 实现TaskRouter路由系统
- [ ] 实现ModelSelector模型选择
- [ ] 实现KnowledgeBase知识库
- [ ] 实现基础智能体（ChineseEditor, Architect, Refactor）
- [ ] 集成LLM（Qwen, DeepSeek, Claude, GPT）
- [ ] 实现基础工具集（文件操作、Git、搜索）

**交付物**:
- ✅ 可工作的核心引擎
- ✅ 基础智能体
- ✅ LLM集成
- ✅ 单元测试覆盖率 > 80%

---

#### 阶段2: 编辑引擎（3-4周）

**目标**: 实现8种编辑模式和diff系统

**任务**:
- [ ] 实现EditBlock模式
- [ ] 实现WholeFile模式
- [ ] 实现UnifiedDiff模式
- [ ] 实现Patch模式
- [ ] 实现Ask模式
- [ ] 实现Architect模式
- [ ] 实现Search模式
- [ ] 实现Help模式
- [ ] 实现Diff预览和应用

**交付物**:
- ✅ 完整的编辑引擎
- ✅ 8种编辑模式
- ✅ Diff系统

---

#### 阶段3: LSP/AST工具（3-4周）

**目标**: 集成LSP和AST-Grep工具

**任务**:
- [ ] 实现LSPManager
- [ ] 集成主流LSP服务器（TypeScript, Python, Go等）
- [ ] 实现LSP工具（diagnostics, rename, goto, references）
- [ ] 实现ASTManager
- [ ] 集成AST-Grep
- [ ] 实现AST搜索和替换
- [ ] 实现Tree-sitter集成

**交付物**:
- ✅ 完整的LSP工具集
- ✅ AST-Grep集成
- ✅ Tree-sitter支持

---

#### 阶段4: Hook和Skill系统（2-3周）

**目标**: 实现可扩展的Hook和Skill系统

**任务**:
- [ ] 实现HookSystem核心
- [ ] 移植oh-my-opencode的25+ Hooks
- [ ] 实现SkillRegistry
- [ ] 移植daoyou的Skill系统
- [ ] 实现Skill加载和触发
- [ ] 实现自定义Hook和Skill支持

**交付物**:
- ✅ Hook系统
- ✅ 25+ 内置Hooks
- ✅ Skill系统
- ✅ 扩展文档

---

#### 阶段5: CLI和TUI（2-3周）

**目标**: 实现命令行和终端UI

**任务**:
- [ ] 实现CLI命令（edit, chat, config等）
- [ ] 实现TUI界面（使用Ink）
- [ ] 实现交互式对话
- [ ] 实现进度显示和通知
- [ ] 实现配置管理
- [ ] 实现历史记录查看

**交付物**:
- ✅ 完整的CLI工具
- ✅ 美观的TUI界面
- ✅ 用户文档

---

#### 阶段6: GUI桌面应用（3-4周）

**目标**: 实现跨平台桌面应用

**任务**:
- [ ] 搭建Electron项目
- [ ] 实现主进程逻辑
- [ ] 实现React渲染进程
- [ ] 实现代码编辑器集成
- [ ] 实现可视化配置
- [ ] 实现历史记录管理
- [ ] 打包和分发

**交付物**:
- ✅ 跨平台桌面应用
- ✅ 安装包（Windows, macOS, Linux）

---

#### 阶段7: 高级功能（4-6周）

**目标**: 实现高级功能和优化

**任务**:
- [ ] 实现并行后台任务
- [ ] 实现Ralph Loop
- [ ] 实现Ultrawork模式
- [ ] 实现团队协作功能
- [ ] 实现知识库共享
- [ ] 实现性能优化
- [ ] 实现成本优化

**交付物**:
- ✅ 高级功能
- ✅ 性能优化
- ✅ 团队协作

---

#### 阶段8: 插件生态（持续）

**目标**: 建立插件生态系统

**任务**:
- [ ] 实现插件API
- [ ] 开发VS Code插件
- [ ] 开发JetBrains插件
- [ ] 开发Vim插件
- [ ] 建立插件市场
- [ ] 编写插件开发文档

**交付物**:
- ✅ 插件系统
- ✅ IDE插件
- ✅ 插件市场

---

### 6.2 里程碑

| 里程碑 | 时间 | 目标 | 状态 |
|--------|------|------|------|
| **M0: 项目启动** | Week 1 | 项目初始化完成 | 🎯 |
| **M1: Alpha版本** | Week 7 | 核心引擎 + 编辑引擎 | 🎯 |
| **M2: Beta版本** | Week 14 | LSP/AST + Hook/Skill + CLI/TUI | 🎯 |
| **M3: RC版本** | Week 18 | GUI + 高级功能 | 🎯 |
| **M4: 1.0正式版** | Week 22 | 完整功能 + 文档 + 测试 | 🎯 |
| **M5: 1.1版本** | Week 26+ | 插件生态 + 社区建设 | 🎯 |

### 6.3 团队配置

**推荐团队规模**: 3-5人

```
核心开发者 (2-3人):
  - 负责核心引擎和智能体系统
  - 负责LSP/AST工具集成
  - 负责编辑引擎实现

前端开发者 (1人):
  - 负责CLI/TUI开发
  - 负责GUI桌面应用
  - 负责用户体验优化

文档工程师 (1人):
  - 负责中英文文档
  - 负责示例和教程
  - 负责社区运营
```

### 6.4 资源需求

**开发资源**:
- 开发机器: 3-5台（macOS/Windows/Linux）
- 服务器: 1台（用于CI/CD和测试）
- 域名和托管: GitHub Pages + 自定义域名

**API成本**:
- LLM API: $500-1000/月（开发测试）
- 其他API: $100/月

**总预算**: $10,000-20,000（6个月）



---

## 7. 差异化优势

### 7.1 相比三个原项目的优势

#### vs daoyouCodePilot

| 维度 | daoyouCodePilot | daoyoucode | 优势 |
|------|----------------|-------------|------|
| **多智能体** | ❌ | ✅ | 支持多智能体协作 |
| **LSP重构** | ❌ | ✅ | 完整LSP重构工具 |
| **并行任务** | ❌ | ✅ | 后台并行执行 |
| **技术栈** | Python | TypeScript | 更好的生态和性能 |
| **中文优化** | ✅ | ✅ | 保持中文优先 |
| **国产LLM** | ✅ | ✅ | 保持深度优化 |

#### vs oh-my-opencode

| 维度 | oh-my-opencode | daoyoucode | 优势 |
|------|----------------|-------------|------|
| **中文支持** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 原生中文支持 |
| **开箱即用** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 更低学习曲线 |
| **独立性** | ⭐⭐ (依赖OpenCode) | ⭐⭐⭐⭐⭐ | 完全独立 |
| **配置复杂度** | 高 | 中 | 更简单的配置 |
| **多智能体** | ✅ | ✅ | 保持强大能力 |
| **LSP工具** | ✅ | ✅ | 保持完整工具集 |

#### vs OpenCode

| 维度 | OpenCode | daoyoucode | 优势 |
|------|----------|-------------|------|
| **中文优化** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 深度中文优化 |
| **内置功能** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 更多开箱即用功能 |
| **智能体** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 更多专业智能体 |
| **开源** | ✅ | ✅ | 保持开源 |
| **扩展性** | ✅ | ✅ | 保持可扩展 |
| **模型无关** | ✅ | ✅ | 保持灵活性 |

### 7.2 独特卖点

#### 🎯 卖点1: 真正的中文优先

```
不是翻译，而是原生中文：
  ✅ 中文Prompt工程优化
  ✅ 中文指令理解增强
  ✅ 中文文档和社区
  ✅ 中文错误提示
  ✅ 中文代码注释理解
```

#### 🎯 卖点2: 智能成本优化

```
自动选择最优模型：
  ✅ 简单任务 → 国产便宜模型
  ✅ 复杂任务 → 国际强大模型
  ✅ 中文任务 → 中文优化模型
  ✅ 成本预估和控制
  ✅ Token使用统计
```

#### 🎯 卖点3: 渐进式复杂度

```
三种使用模式：
  🟢 新手模式: 开箱即用，零配置
  🟡 专业模式: 高级功能，可配置
  🔴 专家模式: 完全定制，可扩展
```

#### 🎯 卖点4: 团队协作优化

```
为团队设计：
  ✅ 配置共享和同步
  ✅ 知识库共享
  ✅ 代码模式学习
  ✅ 团队最佳实践
  ✅ 统一工作流
```

#### 🎯 卖点5: 完整工具链

```
一站式解决方案：
  ✅ 代码编辑（8种模式）
  ✅ 代码重构（LSP工具）
  ✅ 代码探索（AST搜索）
  ✅ 架构分析（智能体）
  ✅ 文档查找（Librarian）
  ✅ Git集成（自动提交）
  ✅ 测试运行（自动测试）
```

### 7.3 目标用户

#### 主要用户群

1. **中文开发者** (70%)
   - 国内互联网公司
   - 独立开发者
   - 开源项目维护者

2. **追求生产力的专业开发者** (20%)
   - 需要高级功能
   - 愿意学习配置
   - 追求极致效率

3. **开发团队** (10%)
   - 需要统一工作流
   - 需要知识共享
   - 需要成本控制

#### 用户画像

**画像1: 张三 - 全栈开发者**
```
背景: 5年经验，独立开发者
需求: 快速开发原型，中文指令，成本敏感
使用: 新手模式，国产模型，CLI工具
```

**画像2: 李四 - 架构师**
```
背景: 10年经验，技术负责人
需求: 代码重构，架构分析，团队协作
使用: 专业模式，多智能体，LSP工具
```

**画像3: 王五 - 开源维护者**
```
背景: 开源项目维护者
需求: 代码审查，文档编写，社区支持
使用: 专家模式，自定义扩展，完全定制
```

### 7.4 竞争优势

#### 技术优势

1. **融合创新**: 集三家之长，避三家之短
2. **中文优先**: 深度优化，不是简单翻译
3. **智能路由**: 自动选择最优方案
4. **成本优化**: 智能模型选择，降低成本
5. **完整工具链**: 一站式解决方案

#### 生态优势

1. **开源社区**: MIT许可，社区驱动
2. **插件生态**: 可扩展，可定制
3. **文档完善**: 中英文文档，示例丰富
4. **持续更新**: 跟进最新技术和模型

#### 商业优势

1. **免费开源**: 核心功能完全免费
2. **企业版**: 可选的企业功能（团队协作、私有部署）
3. **咨询服务**: 提供定制开发和咨询
4. **培训服务**: 提供企业培训

### 7.5 发展路线

#### 短期目标（6个月）

- ✅ 发布1.0正式版
- ✅ 建立中文社区
- ✅ 完善文档和示例
- ✅ 获得1000+ GitHub Stars

#### 中期目标（1年）

- ✅ 发布2.0版本（企业功能）
- ✅ 建立插件生态
- ✅ 获得10000+ GitHub Stars
- ✅ 获得企业客户

#### 长期目标（2年+）

- ✅ 成为中文AI编程助手首选
- ✅ 建立完整生态系统
- ✅ 获得100000+ 用户
- ✅ 实现商业化

---

## 8. 总结

### 8.1 核心价值

daoyoucode 通过融合 daoyouCodePilot、oh-my-opencode、OpenCode 三者的优势，打造一个：

1. **中文优先** 的AI编程助手
2. **功能强大** 的多智能体系统
3. **开箱即用** 的完整工具链
4. **成本优化** 的智能路由系统
5. **开放扩展** 的插件生态

### 8.2 关键成功因素

1. **技术融合**: 成功整合三个项目的核心技术
2. **中文优化**: 真正的中文原生支持
3. **用户体验**: 降低学习曲线，提升易用性
4. **社区建设**: 建立活跃的开源社区
5. **持续迭代**: 快速响应用户需求

### 8.3 风险和挑战

**技术风险**:
- 多智能体协调复杂度
- LSP集成稳定性
- 性能优化挑战

**市场风险**:
- 竞争对手快速迭代
- 用户习惯难以改变
- 商业化路径不明确

**应对策略**:
- 分阶段实施，降低风险
- 快速迭代，及时调整
- 社区驱动，用户共建
- 保持开源，建立信任

### 8.4 下一步行动

1. **立即开始**: 创建项目仓库，搭建基础架构
2. **组建团队**: 招募核心开发者
3. **制定计划**: 细化开发计划和时间表
4. **启动开发**: 按阶段推进开发
5. **社区建设**: 建立GitHub、Discord、微信群

---

**项目愿景**: 让AI编程助手真正为中文开发者服务，让每个开发者都能享受AI带来的生产力提升。

**项目使命**: 融合最佳实践，打造世界级的开源AI编程助手。

**项目口号**: 融合创新，智能编程，中文优先！

