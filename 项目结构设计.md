# daoyoucode 项目结构设计

> **设计原则**: 模块化、插件化、接口清晰、易扩展  
> **技术栈**: Python后端 + React前端 + 插件系统

---

## 📐 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                     用户交互层                               │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  │
│  │   CLI    │  │   TUI    │  │   GUI    │  │   Web    │  │
│  │ (Python) │  │ (React)  │  │(Electron)│  │ (React)  │  │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘  │
│       │             │             │             │          │
└───────┼─────────────┼─────────────┼─────────────┼──────────┘
        │             │             │             │
        └─────────────┴─────────────┴─────────────┘
                      │
┌─────────────────────▼─────────────────────────────────────┐
│                  API网关层 (FastAPI)                       │
│  • REST API                                                │
│  • WebSocket (实时通信)                                    │
│  • 认证授权                                                │
└─────────────────────┬─────────────────────────────────────┘
                      │
┌─────────────────────▼─────────────────────────────────────┐
│                  核心服务层 (Python)                       │
│                                                            │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐   │
│  │ 编排服务     │  │ 智能体服务   │  │ 工具服务     │   │
│  │ Orchestrator │  │ Agents       │  │ Tools        │   │
│  └──────────────┘  └──────────────┘  └──────────────┘   │
│                                                            │
└─────────────────────┬─────────────────────────────────────┘
                      │
┌─────────────────────▼─────────────────────────────────────┐
│                  插件系统 (独立模块)                       │
│                                                            │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐ │
│  │ LSP插件  │  │ Git插件  │  │ AST插件  │  │ 自定义... │ │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘ │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

---

## 📁 详细目录结构

```
daoyoucode/
│
├── backend/                          # Python后端
│   ├── pyproject.toml               # Python项目配置
│   ├── requirements.txt             # 依赖
│   │
│   ├── daoyoucode/                  # 主包
│   │   ├── __init__.py
│   │   │
│   │   ├── api/                     # API层
│   │   │   ├── __init__.py
│   │   │   ├── main.py             # FastAPI主入口
│   │   │   ├── routes/             # 路由
│   │   │   │   ├── agents.py       # 智能体API
│   │   │   │   ├── tasks.py        # 任务API
│   │   │   │   ├── files.py        # 文件操作API
│   │   │   │   └── plugins.py      # 插件API
│   │   │   ├── websocket/          # WebSocket
│   │   │   │   └── handlers.py     # 实时通信
│   │   │   └── middleware/         # 中间件
│   │   │       ├── auth.py         # 认证
│   │   │       └── cors.py         # CORS
│   │   │
│   │   ├── core/                    # 核心服务
│   │   │   ├── __init__.py
│   │   │   ├── orchestrator.py     # 编排器
│   │   │   ├── router.py           # 任务路由
│   │   │   ├── model_selector.py   # 模型选择
│   │   │   └── knowledge_base.py   # 知识库
│   │   │
│   │   ├── agents/                  # 智能体
│   │   │   ├── __init__.py
│   │   │   ├── base.py             # 基类
│   │   │   ├── chinese_editor.py   # 中文编辑
│   │   │   ├── architect.py        # 架构顾问
│   │   │   ├── refactor.py         # 重构专家
│   │   │   └── explore.py          # 代码探索
│   │   │
│   │   ├── tools/                   # 工具集
│   │   │   ├── __init__.py
│   │   │   ├── file_ops.py         # 文件操作
│   │   │   ├── code_search.py      # 代码搜索
│   │   │   ├── git_ops.py          # Git操作
│   │   │   └── test_runner.py      # 测试运行
│   │   │
│   │   ├── llm/                     # LLM集成
│   │   │   ├── __init__.py
│   │   │   ├── base.py             # 基类
│   │   │   ├── qwen.py             # 通义千问
│   │   │   ├── deepseek.py         # DeepSeek
│   │   │   ├── claude.py           # Claude
│   │   │   └── openai.py           # OpenAI
│   │   │
│   │   ├── plugins/                 # 插件管理
│   │   │   ├── __init__.py
│   │   │   ├── manager.py          # 插件管理器
│   │   │   ├── loader.py           # 插件加载器
│   │   │   └── registry.py         # 插件注册表
│   │   │
│   │   ├── skills/                  # Skill系统
│   │   │   ├── __init__.py
│   │   │   ├── registry.py         # Skill注册表
│   │   │   └── loader.py           # Skill加载器
│   │   │
│   │   ├── hooks/                   # Hook系统
│   │   │   ├── __init__.py
│   │   │   ├── manager.py          # Hook管理器
│   │   │   └── builtin/            # 内置Hooks
│   │   │
│   │   ├── storage/                 # 存储层
│   │   │   ├── __init__.py
│   │   │   ├── database.py         # 数据库
│   │   │   ├── cache.py            # 缓存
│   │   │   └── history.py          # 历史记录
│   │   │
│   │   └── utils/                   # 工具函数
│   │       ├── __init__.py
│   │       ├── logger.py           # 日志
│   │       ├── config.py           # 配置
│   │       └── helpers.py          # 辅助函数
│   │
│   ├── cli/                         # CLI工具 (Python)
│   │   ├── __init__.py
│   │   ├── main.py                 # CLI入口
│   │   ├── commands/               # 命令
│   │   │   ├── edit.py
│   │   │   ├── chat.py
│   │   │   └── config.py
│   │   └── ui/                     # CLI UI
│   │       ├── progress.py         # 进度条
│   │       └── formatter.py        # 格式化
│   │
│   └── tests/                       # 测试
│       ├── unit/
│       ├── integration/
│       └── e2e/
│
├── frontend/                         # 前端 (React + TypeScript)
│   ├── package.json
│   ├── tsconfig.json
│   │
│   ├── packages/                    # Monorepo
│   │   │
│   │   ├── shared/                  # 共享代码
│   │   │   ├── src/
│   │   │   │   ├── api/            # API客户端
│   │   │   │   ├── types/          # 类型定义
│   │   │   │   ├── hooks/          # React Hooks
│   │   │   │   ├── components/     # 共享组件
│   │   │   │   └── utils/          # 工具函数
│   │   │   └── package.json
│   │   │
│   │   ├── tui/                     # 终端UI (React Ink)
│   │   │   ├── src/
│   │   │   │   ├── App.tsx         # 主应用
│   │   │   │   ├── components/     # TUI组件
│   │   │   │   ├── screens/        # 屏幕
│   │   │   │   └── index.tsx       # 入口
│   │   │   └── package.json
│   │   │
│   │   ├── web/                     # Web应用
│   │   │   ├── public/
│   │   │   ├── src/
│   │   │   │   ├── App.tsx
│   │   │   │   ├── pages/          # 页面
│   │   │   │   ├── components/     # 组件
│   │   │   │   ├── layouts/        # 布局
│   │   │   │   └── index.tsx
│   │   │   └── package.json
│   │   │
│   │   └── desktop/                 # 桌面应用 (Electron)
│   │       ├── electron/           # Electron主进程
│   │       │   ├── main.ts
│   │       │   └── preload.ts
│   │       ├── src/                # React渲染进程
│   │       │   ├── App.tsx
│   │       │   ├── pages/
│   │       │   └── components/
│   │       └── package.json
│   │
│   └── turbo.json                   # Turbo配置
│
├── plugins/                          # 插件系统 (独立项目)
│   │
│   ├── lsp-plugin/                  # LSP插件
│   │   ├── pyproject.toml
│   │   ├── src/
│   │   │   ├── __init__.py
│   │   │   ├── manager.py          # LSP管理器
│   │   │   ├── servers/            # LSP服务器
│   │   │   └── tools/              # LSP工具
│   │   └── README.md
│   │
│   ├── git-plugin/                  # Git插件
│   │   ├── pyproject.toml
│   │   ├── src/
│   │   │   ├── __init__.py
│   │   │   ├── manager.py
│   │   │   └── operations.py
│   │   └── README.md
│   │
│   ├── ast-plugin/                  # AST插件
│   │   ├── pyproject.toml
│   │   ├── src/
│   │   │   ├── __init__.py
│   │   │   ├── parser.py
│   │   │   └── search.py
│   │   └── README.md
│   │
│   └── template/                    # 插件模板
│       ├── pyproject.toml
│       ├── src/
│       │   └── __init__.py
│       └── README.md
│
├── skills/                           # 内置Skills
│   ├── editblock/
│   ├── wholefile/
│   ├── git-master/
│   └── playwright/
│
├── docs/                             # 文档
│   ├── zh-CN/                       # 中文文档
│   │   ├── getting-started.md
│   │   ├── architecture.md
│   │   ├── api-reference.md
│   │   └── plugin-development.md
│   ├── en-US/                       # 英文文档
│   └── images/
│
├── examples/                         # 示例
│   ├── basic-usage/
│   ├── custom-agent/
│   ├── custom-plugin/
│   └── api-integration/
│
├── scripts/                          # 脚本
│   ├── setup.sh                     # 环境设置
│   ├── build.sh                     # 构建
│   ├── test.sh                      # 测试
│   └── deploy.sh                    # 部署
│
├── .github/                          # GitHub配置
│   ├── workflows/
│   │   ├── ci.yml
│   │   └── release.yml
│   └── ISSUE_TEMPLATE/
│
├── .gitignore
├── README.md
├── LICENSE
├── CONTRIBUTING.md
├── package.json                      # 根package.json (前端)
└── 融合系统架构设计方案.md
```

---

## 🎯 各层职责

### 1. CLI (Python)
```python
# 命令行工具，直接调用后端API
daoyoucode edit main.py "添加日志"
daoyoucode chat
daoyoucode config
```

**特点**:
- Python实现，轻量快速
- 直接调用本地后端服务
- 适合快速操作

### 2. TUI (React Ink)
```typescript
// 终端UI，通过API与后端通信
// 提供更丰富的交互体验
```

**特点**:
- React组件化
- 美观的终端界面
- 实时更新

### 3. Web (React)
```typescript
// Web应用，浏览器访问
// http://localhost:3000
```

**特点**:
- 完整的Web体验
- 可视化配置
- 团队协作

### 4. Desktop (Electron + React)
```typescript
// 桌面应用，跨平台
// 集成代码编辑器
```

**特点**:
- 原生体验
- 离线可用
- 系统集成

---

## 🔌 插件系统设计

### 插件接口定义

```python
# plugins/base.py
from abc import ABC, abstractmethod
from typing import Any, Dict

class BasePlugin(ABC):
    """插件基类"""
    
    name: str
    version: str
    description: str
    
    @abstractmethod
    def initialize(self, config: Dict[str, Any]) -> None:
        """初始化插件"""
        pass
    
    @abstractmethod
    def execute(self, action: str, params: Dict[str, Any]) -> Any:
        """执行插件功能"""
        pass
    
    @abstractmethod
    def cleanup(self) -> None:
        """清理资源"""
        pass
```

### 插件示例

```python
# plugins/lsp-plugin/src/__init__.py
from daoyoucode.plugins.base import BasePlugin

class LSPPlugin(BasePlugin):
    name = "lsp-plugin"
    version = "1.0.0"
    description = "LSP集成插件"
    
    def initialize(self, config):
        # 初始化LSP服务器
        pass
    
    def execute(self, action, params):
        if action == "diagnostics":
            return self.get_diagnostics(params["file"])
        elif action == "rename":
            return self.rename_symbol(params)
        # ...
    
    def cleanup(self):
        # 关闭LSP服务器
        pass
```

---

## 🌐 API设计

### REST API

```python
# backend/daoyoucode/api/routes/tasks.py
from fastapi import APIRouter

router = APIRouter(prefix="/api/v1/tasks")

@router.post("/")
async def create_task(task: TaskCreate):
    """创建任务"""
    pass

@router.get("/{task_id}")
async def get_task(task_id: str):
    """获取任务状态"""
    pass

@router.post("/{task_id}/cancel")
async def cancel_task(task_id: str):
    """取消任务"""
    pass
```

### WebSocket

```python
# backend/daoyoucode/api/websocket/handlers.py
from fastapi import WebSocket

async def handle_task_stream(websocket: WebSocket):
    """实时任务流"""
    await websocket.accept()
    
    async for message in websocket.iter_text():
        # 处理消息
        # 推送实时更新
        await websocket.send_json({
            "type": "progress",
            "data": {...}
        })
```

---

## 🔄 数据流

### 用户操作流程

```
用户 (CLI/TUI/Web/Desktop)
    ↓
API网关 (FastAPI)
    ↓
核心服务 (Orchestrator)
    ↓
智能体/工具/插件
    ↓
LLM服务
    ↓
返回结果
    ↓
实时推送 (WebSocket)
    ↓
用户界面更新
```

---

## 🚀 启动方式

### 开发模式

```bash
# 1. 启动后端
cd backend
python -m daoyoucode.api.main

# 2. 启动前端 (选择一个)
cd frontend

# TUI
pnpm --filter tui dev

# Web
pnpm --filter web dev

# Desktop
pnpm --filter desktop dev

# 3. CLI直接使用
daoyoucode edit main.py "添加日志"
```

### 生产模式

```bash
# 后端
uvicorn daoyoucode.api.main:app --host 0.0.0.0 --port 8000

# 前端
# Web: 构建静态文件，Nginx托管
# Desktop: 打包成可执行文件
```

---

## 💡 扩展新功能

### 添加新智能体

```python
# backend/daoyoucode/agents/my_agent.py
from daoyoucode.agents.base import BaseAgent

class MyAgent(BaseAgent):
    name = "my-agent"
    
    async def execute(self, task):
        # 实现逻辑
        pass
```

### 添加新插件

```bash
# 1. 复制插件模板
cp -r plugins/template plugins/my-plugin

# 2. 实现插件逻辑
# 3. 注册插件
# 4. 使用插件
```

### 添加新前端页面

```typescript
// frontend/packages/web/src/pages/MyPage.tsx
export default function MyPage() {
  return <div>My New Page</div>
}
```

---

## 📊 技术栈总结

| 层级 | 技术 | 说明 |
|------|------|------|
| **CLI** | Python + Click | 命令行工具 |
| **TUI** | React + Ink | 终端UI |
| **Web** | React + Vite | Web应用 |
| **Desktop** | Electron + React | 桌面应用 |
| **后端** | Python + FastAPI | API服务 |
| **核心** | Python | 业务逻辑 |
| **插件** | Python | 独立模块 |
| **数据库** | SQLite/PostgreSQL | 数据存储 |
| **缓存** | Redis (可选) | 缓存 |
| **通信** | REST + WebSocket | 前后端通信 |

---

## ✅ 优势总结

1. **模块化**: 每个部分都是独立模块
2. **接口清晰**: REST API + WebSocket
3. **易扩展**: 插件系统支持任意扩展
4. **技术栈统一**: 
   - 后端全Python（利用现有代码）
   - 前端全React（统一UI技术栈）
5. **灵活部署**:
   - 本地开发：后端+前端分离
   - 生产环境：可以打包成单一应用
6. **新想法友好**: 
   - 新功能作为插件开发
   - 不影响核心代码
   - 可以随时启用/禁用

